## 1. Baseline and approved policy

- [x] 1.1 Inventory deployed Agent Server Store public operations, checkpoint boundaries, namespace/list/filter behavior, update guarantees, verified identity, and non-semantic lexical mechanisms; separate observed facts from proposals. (The 2026-08-29 deployed `langgraph-api==0.13.0` inventory and cleaned synthetic probe recorded SDK and injected-Store interfaces, prefix/list/filter/offset behavior, backend ordering, last-write-wins replacement, absent CAS, checkpoint separation, no native lexical behavior with indexing disabled, and the still-unconfigured deployed identity boundary. See `design.md`.)
- [x] 1.2 Map Agent Server-owned PostgreSQL objects and application-owned storage, and prove application identities cannot directly select or write internal tables. (The read-only catalog audit maps the current internal relations and the separate `session_catalog` projection. Phase 5 source imports no SQL client or database URI, model-visible tool schemas expose no persistence/runtime scope, and focused tests confirm raw Phase 5 Store handlers deny direct access. The trusted Agent Server service necessarily accesses its own persistence through `DATABASE_URI`; no claim is made that its internal table names are a stable privilege API.)
- [x] 1.3 Record the human-approved private-installation policy: separate database per person; opaque server tenant; `local-installation-v1`; server-configured `person` owner; no shared/work memory; owner read, explicit write, exact delete, exact restore, and permanent delete; Jasper/Coder/Librarian memory delegation but not OCR; documentation read for Jasper/Coder/Librarian/OCR. Governance reference: `GOVERNANCE_FRAMEWORK.md`.
- [x] 1.4 Record the human-approved memory kinds and bounds, no automatic eviction, immediate exclusion after deletion, exact seven-day owner restore followed by permanent purge, immediate owner permanent delete, and owner-only content-free audits retained 90 days. Governance reference: `GOVERNANCE_FRAMEWORK.md`.
- [x] 1.5 Record the human-approved bounded same-word memory retrieval policy, Store-native semantic documentation retrieval through local Ollama `embeddinggemma` inside `local-installation-v1`, and documentation ingestion decisions: approved source/artifact classes, existing-supervisor-to-OCR routing, empty production corpus, and synthetic test fixtures.

## 2. Authorization and namespace isolation

- [x] 2.1 Implement a trusted authorization context using only server-derived installation, tenant, trust-domain, owner, capability, corpus, correlation, and delegation-expiry values; reject caller overrides.
- [x] 2.2 Implement distinct namespace encoders for memory and documentation with normalization and hard limits.
- [ ] 2.3 Enforce independent memory read/write/exact-delete/restore/permanent-delete permissions and explicit corpus read/write delegation; reject shared, work, mixed-scope, and unverified requests. (Source and deployed checks use documented thread-create, `threads.create_run`, authenticated Runtime/ToolRuntime, and custom-route shapes. The one configured owner is authenticated before assistant runs; thread operations remain owner-filtered; every Phase 5 node, tool, and route independently rechecks server-derived owner and scope. Direct raw Phase 5 Store access is denied while legacy access is unchanged. Independent grants, explicit corpus delegation, OCR memory denial, and shared/work rejection are source-tested. Full deployed specialist-grant provisioning remains pending, so this rollout task stays unchecked.)
- [x] 2.4 Implement pagination only if paginated capability operations are exposed. Current completed operations reject over-limit requests and expose no pagination token. Unused HMAC capability-token primitives were removed; supervisor corpus-write delegation remains an in-process trusted authority object.
- [ ] 2.5 Test every namespace dimension and prove no cross-scope enumeration, existence leak, partial authorization, or filter-after-fetch. (Source tests now assert exact family/tenant/trust-domain/owner-type/owner-ID/kind-or-corpus/record-type encodings; seed a sibling at each dimension and prove each capability issues only its fully authorized namespace-prefix search; reject mixed-scope, disallowed scope-filter, malformed query, and unauthorized-corpus requests before Store access; distinguish post-authorization metadata result filtering from authorization filtering; and return identical no-Store-access denials for known-shaped and missing exact identifiers. The audit namespace now includes trust domain. Real deployed authorization and existence-oracle acceptance remains required, so this task stays unchecked.)

## 3. Cross-session memory

- [x] 3.1 Implement bounded provenance-bearing memory envelopes as exactly one current Store item per ID, with deterministic sequential identical retry and no revision/operation namespaces, read-back reconciliation, CAS, or checkpoint/Store atomicity claim.
- [x] 3.2 Implement scoped exact, metadata, and bounded lexical reads with deterministic ordering and truthful match-mode labels.
- [x] 3.3 Prove checkpoints, threads, messages, tools, reports, and artifacts never become memory without a separate authorized write. (An isolated graph/checkpointer contract persists synthetic messages, tool results, reports, and artifacts while the memory Store family remains empty, then confirms only a separate authorized capability write creates memory.)
- [x] 3.4 Implement exact deletion by overwriting the one item with per-item TTL, immediate normal-read exclusion, exact owner restore through the inclusive day-seven cutoff with TTL clearing, post-cutoff denial, and physical exact permanent delete. Native asynchronous sweeping replaces application purge scheduling.
- [x] 3.5 Add contract tests for fields, provenance, prohibited content, bounds, no partial writes, retries, stale revisions, lifecycle boundaries, no eviction, and installation isolation. (Focused synthetic contracts cover stale lifecycle rejection without mutation, same-Store tenant/owner isolation, exact normative kind labels with safely encoded namespace segments, and a 1,002-record regression proving internal 1,000-item Store pagination enforces the complete 15 MiB kind limit without expanding the model-facing 1,000-candidate retrieval bound.)

## 4. Documentation retrieval and ingestion

- [x] 4.1 Implement a separate application-owned Store corpus adapter with no fallback to memory and no direct Agent Server internal-table access.
- [x] 4.2 Implement bounded exact, allowlisted canonical metadata, and Store-native semantic documentation retrieval against synthetic fixtures, preserving Store score/order and provenance without document lexical ranking. The documented custom async embedding adapter uses local `embeddinggemma` at 768 dimensions with `keep_alive=0`; deployed empty-corpus semantic search succeeded and `ollama ps` confirmed the model was not resident afterward.
- [x] 4.3 Treat documents as untrusted data and enforce lifecycle-at-read; retrieved content cannot select capability scope.
- [x] 4.4 Implement the narrowly approved human ingestion paths: the authenticated installation owner may explicitly select a supported browser file or submit a bounded public HTTPS page/PDF from Installation Library. Auth-first custom Phase 5 routes obtain the injected `BaseStore` with documented `get_store()` and derive all scope, authority, identity, source, status, and routing values server-side. The file path retains the existing opaque sidecar boundary. The public path uses a trusted bounded, no-proxy, redirect-revalidating, all-global-DNS/IP-pinned TLS downloader, then preserves exact downloaded bytes through that same opaque upload boundary. Both invoke existing Docling OCR and `supervisor_ingest_document`, write complete ordered fragments under the 1,800-byte RAG ceiling, return only document ID/count, and do not auto-link. These downloader controls are narrow and do not claim arbitrary-network safety.
- [ ] 4.5 Implement production trusted handoffs for Librarian public/private/network sources and Coder artifacts. Current source-policy unit tests exercise candidate validation and fail-closed behavior only; no production caller supplies a genuinely trusted specialist handoff. These specialist paths remain unreachable and MUST NOT be represented as complete. The completed manual owner upload and public-URL callers are not Librarian/Coder handoffs.

## 5. Credentials, audit, and safety

- [ ] 5.1 Provision least privilege so memory and documentation cannot mutate each other or access Agent Server internal tables. (Source-level default-deny capabilities and server-derived namespaces separate both families, and neither capability has a SQL path. PostgreSQL cannot distinguish namespaces behind the one Agent Server Store connection, so Phase 5 adds no custom roles, ownership SQL, RLS, direct adapter, or Store infrastructure. Deployed authorization and credential-boundary proof remains pending.)
- [ ] 5.2 Prove persistence/source credentials never enter agent requests, contexts, results, errors, or telemetry. (Source-isolated tests use only synthetic PostgreSQL/source connection strings, API keys, and every common secret field in the capability denylist. They verify actual LangChain tool-call schemas omit runtime, Store, authority, namespace, tenant, owner, connection, and credential fields; model-supplied arguments cannot select those hidden fields; every common secret metadata field is rejected before a write; backend-only credential attributes and credential environment values do not enter normal tool results, ToolRuntime state/context, graph state/checkpoints, sanitized tool/ingestion errors, or bounded Phase 5 audit events; and backend exceptions containing synthetic raw connection strings and named password/API-key/authorization values yield only fixed failure classes while denied audits remain credential-free. The graph contracts use the documented injected `ToolRuntime`/`BaseStore`, `StateGraph`, and `InMemorySaver` boundaries and do not add logging or scanning infrastructure. Source isolation cannot inspect deployed model requests, runtime context, results, exception serialization, Agent Server/LangSmith traces, or process/container logs, so deployed end-to-end inspection remains required and this task stays unchecked.)
- [x] 5.3 Implement bounded content-free owner-only memory audits with exact logical 90-day retention, per-item native TTL, and sanitized documentation access audits under approved policy. (Synthetic contracts verify the exact boundary and immediate logical exclusion, TTL arguments, bounded event fields, absence of payload/query/credential fields, documentation read/write scope and match-mode events, and denial of audit reads to a non-owner. Native sweeping replaces manual pruning.)
- [x] 5.4 Add spoofing, permission separation, pagination, backend-failure, injection, and existence-oracle tests using only isolated synthetic fixtures. (Focused tests reject unverified/caller-selected scope, exercise independent grants, prove no offset/page-token surface, sanitize memory and ingestion backend failures, keep injected document instructions from widening capability access, and prove unauthorized known-shaped/missing exact lookups return the same denial without touching Store.)

## 6. Release verification

- [ ] 6.1 Run capability, isolation, authorization, bounds, provenance, lifecycle, retry/race, lexical-ranking, ingestion, audit, credential-leak, and internal-table-isolation tests and record exact results. (The corrected focused source suite passes `227` tests with 27 third-party deprecation warnings. It covers complete paginated 15 MiB accounting beyond 1,000 records, exact memory kinds, canonical multi-fragment books, authenticated custom routes, lifecycle/audits, owner upload, bounded IP-pinned public download, zero-keep-alive embeddings, links, and credential boundaries. Deployed checks confirm auth-first 401/200 behavior, empty metadata and semantic routes, direct raw Phase 5 Store denial, sanitized forged/private-source denials, temporary-thread link authorization and cleanup, 768-dimensional local embeddings, and immediate `embeddinggemma` unload. Ordinary BaseStore operations provide no compare-and-swap or multi-key transaction, so no conflicting-write guarantee is claimed. Real successful book/public ingestion, native TTL sweep timing, full specialist-grant provisioning, and deployed trace/log credential review remain pending, so this task stays unchecked.)
- [x] 6.2 Run the project-local strict OpenSpec validator and resolve errors before implementation approval. (`npx --yes @fission-ai/openspec@latest validate phase-5-cross-session-memory-and-documentation-rag --strict`: valid.)
- [ ] 6.3 Obtain release approval for the observed Store mechanism, measured limits, Agent Server service-credential boundary, and any production corpus snapshot.
- [x] 6.4 Confirm the implementation retained the existing supervisor graph and added no direct specialist communication, shared memory, MCP, custom/unrelated vector infrastructure, semantic memory behavior, ontology, reindexing workflow, or document/corpus deletion. The only semantic behavior is the documented supplied Store index for documentation fragments. Source inspection and focused tests confirm these boundaries.

## 7. Canonical library and thread-scoped document links

- [x] 7.1 Refactor documentation persistence to exactly one server-derived installation-wide corpus in the Agent Server-injected `BaseStore`; store each canonical document/fragment once, keep document-level tags only in canonical document metadata, and reject per-session content, tag, or metadata copies. Preserve all existing provenance, lifecycle, ingestion, authorization, and empty-production-corpus requirements. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (Source implementation now fixes `installation-docs`, writes one content-free canonical metadata item directly under `record:document` by document ID and one content-bearing item directly under `record:fragment` by fragment ID, normalizes/deduplicates/sorts at most 32 bounded canonical tags, stores no tag/title/content copy on fragments or operation/session records, validates before mutation, and preserves sanitized supervisor/OCR ingestion and canonical lifecycle reads without claiming multi-key atomicity. Focused synthetic Phase 5 suite: `99 passed`.)
- [x] 7.2 Implement stable exact-key document and fragment reads with full authorized namespaces wherever `BaseStore.aget` permits; use the supplied Store's bounded semantic search for content with no exact-lookup-by-enumeration fallback or custom document lexical ranking. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (Recording-Store contracts prove exact-key behavior; semantic retrieval passes the validated query to `BaseStore.asearch` on the authorized fragment namespace, preserves native score/order, and indexes only fragment `content`. Canonical metadata remains `index=False`, and canonical tags are not copied to fragments.)
- [x] 7.3 Route Jasper retrieval through deterministic graph tools using `Runtime.store`, and route human Installation Library reads through authenticated custom FastAPI routes using documented `langgraph.config.get_store()` over the Agent Server-injected `BaseStore`; expose no Phase 5 raw namespace to the browser and make no model call for manual UI reads. Governance reference: `GOVERNANCE_FRAMEWORK.md`.
  - [x] 7.3a Server/auth portion: implement bounded `resolve`, canonical-metadata, native fragment-semantic, and owner-upload routes under auth-first exact installation identity; preserve Store-native score/order, join active canonical metadata, return metadata only, deny direct Phase 5 Store get/search/mutation/listing, and preserve legacy Store rules. Current-thread link operations remain owner-filtered stateful runs.
  - [x] 7.3b UI portion: Installation Library and Session Documents call the authenticated custom routes with the current API URL, `X-Api-Key`, and optional `X-Auth-Scheme`; TanStack keys are partitioned by API URL and a stable non-secret credential discriminator. No fragment body, raw namespace, or credential enters the projection/cache contract.
- [x] 7.4 Add an ordered, idempotent maximum-100-unique-document-ID set to authoritative checkpointed thread state; derive current thread/session identity server-side, restore links on reopen, permit many threads to reference one ID, and ensure links contain no content, tags, title, provenance, or copied metadata and never become cross-session memory. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (`State.session_document_ids` is a `list[str]` using a validating complete-list replacement reducer, never an append reducer. The pure normalizer accepts only a complete list of stable ID strings, preserves first-seen order, deduplicates, rejects more than 100 unique IDs and every object/non-ID payload, and stores no link envelope or metadata. A real `StateGraph` with `InMemorySaver` proves malformed state rejection, same-thread reopen persistence, different-thread isolation, independent same-ID references across many threads, authoritative empty-list replacement, checkpoint field presence, and zero injected-Store writes. Focused task suite: `18 passed`; add/remove authorization, canonical validation, Jasper linking, UI, and deployed checks remain pending in 7.5+.)
- [x] 7.5 Implement owner-authorized manual add/remove for only the current authoritative thread, including exact canonical-document validation before add, no automatic eviction, and removal that leaves the canonical document and every other thread link unchanged. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (The existing `chat_ui` graph now routes a pending ID-only `{action, document_id}` request to one deterministic non-model node. It verifies the authenticated `Runtime.server_info.user` against installation configuration, binds checkpoint mutation to `Runtime.execution_info`/configured current thread, validates add through `StoreCapabilities` exact canonical-document lookup, and replaces the normalized complete list for idempotent bounded add/remove. All failures clear the request and return one sanitized result without changing links. Real `StateGraph`/`InMemorySaver`/`InMemoryStore` tests prove forbidden-scope rejection, exact lookup, active/missing/unavailable behavior, owner and thread binding, idempotency, the 100-ID no-eviction bound, cross-thread removal isolation, no model call, unchanged canonical data, and unchanged no-action chat routing. Focused Phase 5/runtime suite: `170 passed`; Jasper linking, UI, deployment, and tasks 7.6+ remain pending.)
- [x] 7.6 Update Jasper graph behavior so search alone does not link a result, actual excerpt use links the stable document ID to the current authenticated run's authoritative thread, failed authorization/validation/link persistence prevents a claim of use, and linked full documents are never injected into state or model context; retrieve only bounded relevant excerpts when needed. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (Jasper alone now receives one exact fragment-use tool whose only model argument is `fragment_id`; authenticated `ToolRuntime` supplies current state/config and the injected `BaseStore`. The tool verifies current-thread authority, exact-reads one active canonical fragment through `StoreCapabilities`, derives its document ID from the trusted result, and returns the bounded excerpt in a matching-call-ID `ToolMessage` together with a `Command` complete-list replacement normalized by `normalize_session_document_ids`. Search remains non-linking; missing, inactive, unauthorized, malformed, unavailable, or over-bound use returns one fixed sanitized tool failure with no link update. Jasper prompt guidance requires successful exact use before evidentiary reliance, and nested request/state/result plus outer prepare/return projections carry only document IDs without loading linked documents. Focused contracts cover hidden scope, Jasper-only registration, exact retrieval without corpus search/full-document loading, trusted-ID linking, search non-linking, idempotency/bounds, equal inactive/missing failure, matching tool-call IDs, and nested/outer propagation. Full Phase 5/runtime suite: `185 passed`; edited-Python Ruff, LangGraph config validation, strict OpenSpec validation, and `git diff --check` pass.)
- [x] 7.7 Implement only the Installation Library and Session Documents frontend views with the existing TanStack Query provider; use authenticated custom-route calls for deterministic server-authorized reads and explicit owner-upload and bounded public-URL ingestion, retain SDK stateful runs only for current-thread links, keep the server authoritative, invalidate both views after ingestion/link changes, and make no unrelated UI changes. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (Session Documents reads authoritative ordered IDs from thread state, resolves canonical metadata through the Phase 5 route, and sanitizes unavailable references. Installation Library metadata/semantic reads use the same route. Its explicit file chooser posts only the selected file to the existing sidecar, then sends the opaque reference, exact filename/title, and bounded optional tags to the custom ingestion route with bounded status; success does not auto-link. A second URL/title/tags form calls only the authenticated public-document route; the backend performs download and upload-boundary preservation. Add/remove remains an ID-only current-thread run. Cache keys include API URL plus a non-secret credential discriminator; raw credentials and Phase 5 namespaces are absent.)
- [x] 7.8 Add isolated server/graph tests for canonical single-copy and ordered multi-fragment storage, stable exact-key access, bounded native semantic search through server operations/tools, metadata-only UI projection, current-thread authorization, lifecycle filtering, link bounds/idempotency/isolation, Jasper actual-use linking, and no automatic full-content context injection. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (Focused route/service tests inject `get_store()` and authenticated/unauthenticated users, prove lifecycle and content-free audit behavior, metadata-only native ordering, bounds, exact source-byte preservation, content-derived owner-upload deduplication, bounded IP-pinned public downloading with redirect/DNS revalidation, content-derived public identity, no model/authority override, and sanitized failures; graph tests retain current-thread links and agent `Runtime.store` tools. Specialist production handoffs remain pending under 4.5.)
- [x] 7.9 Add frontend tests proving both named views use server-backed TanStack Query flows, manual upload/search/add/remove refreshes both caches, reopening switches to authoritative thread links, stale/forged data cannot authorize mutation, canonical tag changes appear across sessions, and unrelated Session Sources remains separate. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (The fully mocked Playwright contracts prove sidecar upload followed by the authenticated owner-upload route, URL/title/tags submission only to the authenticated public-document route with no automatic thread run, custom metadata/semantic/resolve route payloads with existing auth headers, zero direct Phase 5 Store calls or stateless read/ingestion runs, no automatic link run, metadata-only rendering, ID-only current-thread mutations, both-family cache invalidation, thread reopen/isolation, and unchanged legacy Session Sources behavior. These mocks are source-level UI contracts, not deployed proof. Focused Chromium with a non-building Next development server: `3 passed (14.7s)`.)
- [ ] 7.10 Run focused source and UI suites, deployed authorization/existence-oracle/credential checks, and the project-local strict OpenSpec validator; record exact results and obtain human release approval before enablement. Governance reference: `GOVERNANCE_FRAMEWORK.md`. (Current evidence: `227` focused backend tests pass; focused TypeScript/Prettier/ESLint and strict OpenSpec validation pass; deployed auth-first route checks, empty native semantic search, direct Store denial, sanitized forged/private-source denials, temporary stateful link authorization/cleanup, model unload, and a real port-3002 Chromium empty-library flow pass. The real browser also proves a fresh profile receives the API-key prompt and one click opens Installation Library. Successful real-document ingestion, specialist handoffs, complete deployed credential/trace/log review, and human release approval remain pending.)
