## Cross-change review reminder

Before opening or implementing these tasks, review `../manual-scroll-observation-capture/` as well. This change owns implementation and verification of the conversation scroll behavior under test; the companion owns optional human-centered evidence capture used to observe and report it. Do not conflate implementation with observation, and keep both changes' scopes distinct.

## 1. Live-browser investigation

- [ ] 1.1 Investigate the newly submitted user-message arrival path in a live browser, including insertion and layout-settlement timing; keep the work pending until top positioning is visually verified. Governance reference: `GOVERNANCE_FRAMEWORK.md`. Evidence: source inspection found the human arrival anchor and settled one-shot positioning path in `agent-chat-ui/src/components/thread/message-list.tsx`; focused browser verification remains unresolved because the temporary mocked stream fixture did not deliver the arrival event.
- [ ] 1.2 Investigate the arriving assistant-answer path in a live browser, including content growth and layout-settlement timing; keep the work pending until top positioning is visually verified. Governance reference: `GOVERNANCE_FRAMEWORK.md`. Evidence: existing `tests/phase2.spec.ts` confirms the assistant answer renders and exposes answer anchors; focused live scroll verification remains unresolved because the temporary mocked stream fixture did not deliver the arrival event.

## 2. Scroll-control contract verification

- [ ] 2.1 Verify that each new-arrival path performs one top-positioning action after settlement, then preserves user scroll control without bottom-following or repeated repositioning; verify this is not assistant-answer reveal following and does not reinstate removed bottom-lock behavior. Governance reference: `GOVERNANCE_FRAMEWORK.md`. Evidence: source inspection shows cancellation listeners and no bottom-follow loop; live visual verification is still pending for both arrival paths.
- [ ] 2.2 Verify initial reopen placement separately: after a non-empty hydrated message window is mounted, place the viewport at the bottom once; cover empty sessions, loading/history errors, forked/reopened threads, reduced motion, and no invented durable viewport-position feature. Governance reference: `GOVERNANCE_FRAMEWORK.md`. Evidence: `tests/performance.spec.ts` passed against a hydrated 300-message thread and source inspection confirms two-frame one-time bottom placement with `behavior: "auto"`; the full edge-case matrix remains undocumented in a live browser.
- [ ] 2.3 Record automated checks as supporting evidence only and do not mark the behavior complete until both live-browser new-arrival paths and the reopen path are resolved and documented. Governance reference: `GOVERNANCE_FRAMEWORK.md`. Supporting checks: TypeScript passed; `tests/phase2.spec.ts` passed (2 tests); `tests/performance.spec.ts` passed (1 test). `format:check` remains red on 10 pre-existing unrelated files.
