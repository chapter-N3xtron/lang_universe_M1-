## 1. Desktop workspace layout

- [ ] 1.1 Record the approved stable desktop/laptop top-bar, composer-control, mode-control, and tool-call-toggle decision in the change documentation.
- [ ] 1.2 Refactor the desktop/laptop workspace shell and session actions into one normal-flow shared top bar while retaining existing availability, session flows, and persistence.
- [ ] 1.3 Move icon-only Chat, Split, and Visual controls into the desktop composer control row with accessible labels, tooltips, and selected states.

## 2. Composer controls

- [ ] 2.1 Reorganize the desktop composer controls into the approved lower left/right groups while preserving text input, microphone, send, selectors, upload, and repository behavior.
- [ ] 2.2 Replace the labeled tool-call switch with the single accessible icon-only Wrench/Phone visibility toggle backed by existing state.

## 3. Focused validation

- [ ] 3.1 Add or update focused browser coverage for shared top-bar action availability, desktop composer placement, workspace modes, and tool-call visibility behavior.
- [ ] 3.2 Run formatting, TypeScript validation, and focused tests.
